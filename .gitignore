# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment files (contain secrets)
.env
.env.*
.env.local
.env.development
.env.production
.env.test
.env.aws

# AWS Configuration files (contain secrets and account IDs)
aws-config*.json
.aws/
deploy-to-aws.sh

# JWT keys and secrets
*jwt*.txt
*jwt*.pem
*.key
*.pem
*secret*
*Secret*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj*
*.sln
*.sw?

# AI Assistant directories (may contain PII or sensitive context)
.cursor/*
.claude/*
.claude-flow/*
.specify/*
.kiro/*

# Package lock files
package-lock.json

# Temporary and local files
*.local
example_requirements.txt
secrets_del_after.md

# ============================================================================
# SENSITIVE DOCUMENTATION - NEVER COMMIT TO GIT OR DEPLOY TO PRODUCTION
# ============================================================================
# These files contain OAuth callback URLs, AWS account IDs, credentials,
# and deployment information. Keep locally for reference only.

# OAuth setup documentation (contains callback URLs and client secrets)
github-auth.md
google-auth.md
oauth.md
OAUTH_*.md
*OAUTH*.md
convex/OAUTH_SETUP_GUIDE.md

# AWS-specific documentation (contains account IDs, ARNs, and credentials)
AWS-*.md
*AWS*.md
AMPLIFY_*.md
amplify.yml
CROSS-ACCOUNT-*.md
COST_OPTIMIZATION.md
deploy-to-aws.sh

# Cloudflare deployment documentation (contains deployment URLs and config)
CLOUDFLARE_*.md
*CLOUDFLARE*.md

# Deployment documentation (contains sensitive URLs and configuration)
DEPLOY_*.md
DEPLOYMENT*.md
*DEPLOYMENT*.md
SETUP-*.md
*SETUP*.md
COMPLETE-*.md
SIMPLE-*.md

# Session summaries and status files (contains development history)
SESSION_*.md
FINAL-*.md
ACTION-*.md
CONVEX_*.md
REVIEW-*.md
SECURITY_*.md
TASK_*.md
*-errors-*.md
CLEANUP_*.md

# PowerShell scripts (contain hardcoded values and are not used by application)
scripts/*.ps1

# Script documentation (contains OAuth URLs and setup instructions)
scripts/*.md

# Pre-commit checklists and quick guides
PRE-COMMIT-*.md
QUICK_*.md
WARP.md

# Architecture and planning documents
ARCHITECTURE_*.md
architecture_diagram.py
*.gv
agent_builder_architecture*

# Python requirements (not needed for Cloudflare frontend deployment)
requirements_complete.txt

# Documentation directories (keep for reference but don't track)
docs/
specs/
CORRECTED-REVIEW.md
REPOSITORY_CLEANUP_SUMMARY.md
CLEANUP_VERIFICATION.md
scripts/verify-oauth-config.sh
