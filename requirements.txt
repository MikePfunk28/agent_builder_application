# ============================================================================
# CORE AGENT RUNTIME (AWS Bedrock AgentCore)
# ============================================================================
strands-agents-tools>=0.2.11
bedrock-agentcore>=0.0.25
bedrock-agentcore-starter-toolkit>=0.1.25

# Strands Agents Tools - All extras for comprehensive agent capabilities
# These are installed on-demand in Fargate containers (serverless - no cost when idle)
strands-agents-tools[mem0_memory]>=0.2.11
strands-agents-tools[local_chromium_browser]>=0.2.11
strands-agents-tools[agent_core_browser]>=0.2.11
strands-agents-tools[agent_core_code_interpreter]>=0.2.11
strands-agents-tools[a2a_client]>=0.2.11
strands-agents-tools[diagram]>=0.2.11
strands-agents-tools[rss]>=0.2.11
strands-agents-tools[use_computer]>=0.2.11

# ============================================================================
# AWS SDKs
# ============================================================================
boto3>=1.35.0
botocore>=1.35.0

# ============================================================================
# AUTHENTICATION
# ============================================================================
PyJWT>=2.8.0

# ============================================================================
# CONTAINER ORCHESTRATION
# ============================================================================
docker>=6.0.0

# ============================================================================
# MCP (Model Context Protocol) SDK - Python
# ============================================================================
# MCP SDK for building MCP servers and clients
mcp>=1.0.0

# MCP client for agent-to-agent communication (A2A)
# Note: a2a_client is included in strands-agents-tools[a2a_client] above

# ============================================================================
# VECTOR DATABASES & EMBEDDINGS
# ============================================================================
# LanceDB - Serverless vector database
lancedb>=0.5.0

# Mem0 - Memory management for agents
mem0ai>=0.1.0

# Qdrant - Vector search engine
qdrant-client>=1.7.0

# ChromaDB - Embedding database
chromadb>=0.4.0

# Diagram tooling used by docs/diagram.py
graphviz>=0.20.1
matplotlib>=3.7.0
networkx>=3.1
diagrams>=0.24.0

# FastAPI for assistant API endpoints (serverless)
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Pydantic for data validation
pydantic>=2.5.0

# OpenTelemetry for X-Ray tracing (on-demand logging)
opentelemetry-api>=1.22.0
opentelemetry-sdk>=1.22.0
opentelemetry-instrumentation-fastapi>=0.43b0
aws-xray-sdk>=2.12.0

# Logging
python-json-logger>=2.0.7

# Runtime helpers pulled in transitively; keep pinned explicitly
python-dotenv>=1.0.0
watchdog>=6.0.0
flask>=3.1.2
flask-cors>=6.0.1
requests>=2.31.0
python-dateutil>=2.8.2