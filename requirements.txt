# Core agent runtime
strands-agents>=1.0.0
bedrock-agentcore>=1.0.0

# AWS SDKs for Cognito, SSO, and deployments
boto3>=1.28.0

# Authentication utilities
PyJWT>=2.8.0

# Local container orchestration for Ollama tests
docker>=6.0.0

# Diagram tooling used by docs/diagram.py
graphviz>=0.20.1
matplotlib>=3.7.0
networkx>=3.1
diagrams>=0.24.0

# Runtime helpers pulled in transitively; keep pinned explicitly
python-dotenv>=1.0.0
uvicorn>=0.23.0
watchdog>=6.0.0
